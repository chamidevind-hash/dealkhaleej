# Store Logos

The `official` folder contains store image assets obtained from each official store site or its published CDN.

`NordVPN` and `Airalo` remain linked to their official hosted favicon URLs in `data/stores.json` because those sites block direct asset downloads.

New coupon entries can reuse the store logo URL by selecting a store in the admin dashboard.
